/*
In the code shown below, identify the following items:

the function arguments
the function body
the function declaration
the function invocation
the function name
the function parameters
the function return value
the names of all variables in this program
*/

function multiplyNumbers(num1, num2, num3) { //this block is the function declaration, multiplyNumbers is the function name. num1, num2, and num3 are the function parameters
  let result = num1 * num2 * num3; //this is the first line of the function body
  return result; //this is the last line of the function body, result is the function return value.
}

let product = multiplyNumbers(2, 3, 4); //2, 3, 4 are the arguments. multiplyNumbers() is a function invocation.

//The names of all the variables in this program are as follows:
//multiplyNumbers, result, num1, num2, num3, and product.